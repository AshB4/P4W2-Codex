Reminder: some config was moved to a dotfile. gitignore handles it but check if file's there. database_config.py will complain if it can't find what it needs.

`flask run` or `python app.py` to launch.